import 'package:flutter/material.dart';

class AppColors {
  static const primaryRed     = Color(0xFFE50914);
  static const primaryBlack   = Color(0xFF000000);
  static const darkGrey       = Color(0xFF141414);
  static const mediumGrey     = Color(0xFF222222);
  static const lightGrey      = Color(0xFF808080);
  static const white          = Color(0xFFFFFFFF);

  static const inputBackground = Color(0xFF333333);
  static const hintColor       = Color(0xFF8C8C8C);

  /// 24 % opaque white (used for thin borders-in-dark)
  static const white24 = Color(0x3DFFFFFF);   // 0x3D = 24 % alpha
}
